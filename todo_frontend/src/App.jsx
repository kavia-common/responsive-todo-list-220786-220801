import React from "react";

/**
 * Basic placeholder UI so the app can start.
 * The full Todo feature implementation is outside the scope of this dependency update task.
 */
export default function App() {
  return (
    <main className="container">
      <h1 className="title">Todo</h1>
      <p className="subtitle">Dependency update scaffold (React 18 + Vite).</p>
    </main>
  );
}
